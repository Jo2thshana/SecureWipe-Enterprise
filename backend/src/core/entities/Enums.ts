export enum UserRole {
  USER = 'USER',
  ADMIN = 'ADMIN'
}

export enum WipeMode {
  SIMULATION = 'SIMULATION',
  ACTUAL = 'ACTUAL'
}

export enum WipeType {
  FULL = 'FULL',
  SELECTIVE = 'SELECTIVE'
}

export enum VerificationStatus {
  PENDING = 'PENDING',
  PASSED = 'PASSED',
  FAILED = 'FAILED'
}
